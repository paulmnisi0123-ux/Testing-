async function askAI(){let question=document.getElementById("question").value;let answer=document.getElementById("answer");answer.innerText="Thinking...";const res=await fetch("/api/ask",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question})});const data=await res.json();answer.innerText=data.reply;}let time=1500;let interval;function startTimer(){clearInterval(interval);interval=setInterval(()=>{if(time>0){time--;updateTimer();}},1000);}function resetTimer(){clearInterval(interval);time=1500;updateTimer();}function updateTimer(){let mins=Math.floor(time/60);let secs=time%60;document.getElementById("timer").innerText=`${mins}:${secs<10?"0"+secs:secs}`;}function addFlashcard(){let q=document.getElementById("flashQ").value;let a=document.getElementById("flashA").value;let div=document.createElement("div");div.innerHTML=`<p><b>${q}</b><br>${a}</p><hr>`;document.getElementById("flashcards").appendChild(div);}
